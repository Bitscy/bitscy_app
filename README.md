# Bitscy

A Bitcoin-native marketplace for African creative women. List your craft, get paid in Lightning, withdraw straight to your Nigerian bank account.

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](#license) [![Next.js](https://img.shields.io/badge/Next.js-16-black)](https://nextjs.org/) [![TypeScript](https://img.shields.io/badge/TypeScript-strict-blue)](https://www.typescriptlang.org/) [![Tailwind CSS](https://img.shields.io/badge/Tailwind-4-38B2AC)](https://tailwindcss.com/) [![PWA](https://img.shields.io/badge/PWA-installable-5A0FC8)](https://web.dev/progressive-web-apps/)

Bitscy solves a real, lived problem: African creators are structurally excluded from global creative marketplaces. Etsy doesn't cleanly support Nigerian sellers. PayPal blocks Nigerian accounts. Stripe isn't available. International buyers can't pay Nigerian artists directly with their existing tools. Bitcoin Lightning is the only payment system that genuinely doesn't care about nationality, banking jurisdiction, or platform policies.

The protagonist is **Adaeze** — a talented Nigerian artist who paused her craft because she couldn't find buyers or get paid reliably. The product is built for her and the millions of women like her.

---

## Features

### For sellers (Adaeze)

- **One-tap shop creation** — open a shop with a name and a password. No KYC. No bank account required upfront. Identity is a Nostr key, generated and encrypted client-side.
- **Product listings** — title, description, up to 5 photos, price in naira, category. The price round-trips through sats so buyers anywhere on earth can pay.
- **Lightning-native sales** — buyers pay BOLT-11 invoices generated by the platform Breez wallet. The funds land instantly. The seller's balance updates in real time.
- **Order management** — see incoming orders, view the buyer's shipping address (NIP-04 encrypted, decryptable only by the seller), mark as shipped.
- **NGN withdrawals** — withdraw sats to a Nigerian bank account via Bitnob. Real integration on Bitnob's sandbox: real API, real Lightning testnet routing, no real naira moves in v1.
- **Public storefront** — a shareable URL like `bitscy.com/shop/adaeze-studio` that lists every active piece.

### For buyers (Tobi)

- **Browse without signing up** — `/marketplace` is public; only the checkout step requires an account.
- **Pay with any Lightning wallet** — Phoenix, Wallet of Satoshi, Breez, your hardware wallet. Scan the QR or open in your wallet of choice.
- **Self-custodial embedded wallet** *(stretch)* — for buyers without a Lightning wallet, generate a Bitcoin-backed wallet right in the browser via Breez SDK WASM. The mnemonic never leaves the device.
- **Pseudonymous identity** — Nostr key under the hood; no email, no phone, no real-name requirement.
- **Encrypted shipping** — your shipping address is encrypted to the seller's pubkey via NIP-04. Bitscy never sees the plaintext.

### Architecture pillars

- **Zero-knowledge auth.** PBKDF2 + AES-GCM, 210k iterations. The server never receives the password or plaintext Nostr secret key — only an opaque encrypted blob.
- **Postgres for queries, Nostr for verifiability.** Every product is mirrored as a NIP-15 kind 30018 event. Every order as a kind 30019 event. If Bitscy disappeared tomorrow, the catalog would still exist on public relays.
- **Mobile-first PWA.** Installable to home screen on Android and iOS. Works offline (sans payments). No app stores.
- **Real integrations only.** No mocks in v1: real Breez Lightning, real Bitnob (sandbox), real CoinGecko prices, real Nostr relays, real Cloudinary uploads. The only thing that doesn't actually move is real Naira to a real bank — that's gated behind Bitnob KYB, an explicit v2 step.

---

## NIPs (Nostr Implementation Possibilities)

Bitscy implements standard NIPs only — no custom event kinds. The marketplace is interoperable with any NIP-15-aware Nostr client.

### Standard NIPs used

| NIP | Title | Where it shows up |
|---|---|---|
| [NIP-01](https://github.com/nostr-protocol/nips/blob/master/01.md) | Basic protocol | Event creation, signing (Schnorr), publishing to relays. Profile metadata (kind 0). |
| [NIP-04](https://github.com/nostr-protocol/nips/blob/master/04.md) | Encrypted DMs | Buyer encrypts shipping address to seller's pubkey before sending. Bitscy never sees plaintext. |
| [NIP-06](https://github.com/nostr-protocol/nips/blob/master/06.md) | Mnemonic key derivation | 12-word BIP39 phrase → Nostr secret key via `m/44'/1237'/0'/0/0`. Recovery flow. |
| [NIP-15](https://github.com/nostr-protocol/nips/blob/master/15.md) | Marketplace | Kinds 30018 (product) and 30019 (order) — parameterized replaceable events with `d` tag = entity id. |
| [NIP-98](https://github.com/nostr-protocol/nips/blob/master/98.md) | HTTP auth | Login challenge-response. Buyer/seller signs a kind 27235 event to prove control of their key. |

### Event kinds in use

| Kind | Purpose | Where it's built |
|---|---|---|
| `0` | User profile metadata (name, about, picture, lud16) | `src/services/nostr/events.ts` — `buildProfileEventTemplate` |
| `27235` | Login challenge signature (NIP-98 HTTP Auth) | `src/lib/auth/sign.ts` — `signChallenge` |
| `30018` | Product listing (NIP-15, parameterized replaceable, `d` = product id) | `src/services/nostr/events.ts` — `buildProductEventTemplate` |
| `30019` | Order event (NIP-15, content is NIP-04 encrypted shipping) | Commerce service on order settlement |

### Default relays

```
wss://relay.damus.io
wss://nos.lol
wss://relay.primal.net
wss://nostr.wine
```

Configurable via `NEXT_PUBLIC_NOSTR_RELAYS`.

---

## Quick Start

### Prerequisites

- **Node.js** ≥ 22
- **pnpm** 9
- **PostgreSQL** (Supabase, Neon, or local — `DATABASE_URL` + `DIRECT_URL`)
- **Cloudinary** account (free tier; for product image uploads)
- **Breez API key** for Lightning (free, [request here](https://breez.technology/request-api-key/))
- **Bitnob sandbox** account (free, [sandbox.bitnob.co](https://sandbox.bitnob.co)) — required only for the seller withdrawal flow

### Installation

```bash
# 1. Clone
git clone https://github.com/Bitscy/bitscy_app.git
cd bitscy_app

# 2. Install
pnpm install

# 3. Configure
cp .env.example .env.local
# fill in DATABASE_URL, BREEZ_API_KEY, CLOUDINARY_*, SESSION_SECRET, etc.

# 4. Push schema
pnpm db:push

# 5. (Optional) seed demo data
pnpm db:seed

# 6. Run
pnpm dev
```

The app runs at `http://localhost:3000`.

### Generating system keys

```bash
# Generate a 32-byte Nostr secret key (hex) for SYSTEM_NSEC
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Generate a 32-byte session secret for SESSION_SECRET
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

---

## Architecture

Four layers, top to bottom.

```
┌──────────────────────────────────────────────────────────────┐
│ 1. PWA client                                                │
│    Installable mobile-first web app on Android & iOS         │
└──────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────┐
│ 2. Frontend (Next.js App Router + React 19)                  │
│    Pages, components, Zustand stores, client-side crypto,    │
│    optional embedded Breez wallet (browser WASM).            │
└──────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────┐
│ 3. Backend (Next.js API routes)                              │
│    Auth, product CRUD, order state machine, Lightning        │
│    invoice orchestration, Nostr publishing, Bitnob payouts.  │
└──────────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────────┐
│ 4. External infrastructure                                   │
│    Breez SDK Liquid · Bitnob sandbox · CoinGecko · Nostr     │
│    relays · PostgreSQL (Supabase) · Cloudinary · Web Push    │
└──────────────────────────────────────────────────────────────┘
```

### Identity & custody

- **Auth is zero-knowledge.** The client generates a Nostr keypair, derives a 256-bit AES key from the user's password via PBKDF2-SHA256 (210k iterations), encrypts the 12-word recovery mnemonic, and ships `{ npub, encryptedKey, salt, iv }` to the server. No password, no plaintext key.
- **Sellers are platform-custodial in v1.** Funds land in a single Bitscy-operated Breez wallet; per-seller balances are tracked in an append-only `LedgerEntry` table. Sellers can always withdraw to NGN or to a personal Lightning address. v2 migrates each seller to their own self-custodial wallet.
- **Buyers are self-custodial.** Either pay from an existing Lightning wallet (Phoenix, Wallet of Satoshi, etc.) or create a Bitscy-embedded Breez wallet client-side. Mnemonic stays in IndexedDB, encrypted under the user's passphrase.

### Order state machine

```
PENDING ──invoice settled──▶ PAID ──seller marks shipped──▶ SHIPPED
   │                          │
   │                          └──buyer confirms (v2)──▶ DELIVERED
   │
   └──invoice expires / buyer abandons──▶ CANCELLED
```

Atomic updates (`UPDATE … WHERE status = 'PENDING'`) handle the race between the Breez SDK `paymentSucceeded` event and the frontend polling `/api/lightning/verify`.

### Authoritative spec

The complete end-to-end architecture lives in [`Bitscy_Integration_Flow.md`](Bitscy_Integration_Flow.md) — the authoritative spec for the whole system.

---

## Project Structure

```
bitscy/
├── prisma/
│   └── schema.prisma                  # User, Product, Order, LedgerEntry, Payout, BankAccount, PendingPayment, PushSubscription
├── public/
│   ├── manifest.json                  # PWA manifest
│   ├── icons/                         # Home-screen icons
│   └── artwork-*.jpg                  # Demo product images (seed data)
├── src/
│   ├── app/                           # Next.js App Router
│   │   ├── api/                       # Backend routes (see Configuration → API)
│   │   ├── (marketplace)/             # Public browse + product detail
│   │   ├── checkout/[orderId]/        # Lightning invoice + polling + success
│   │   ├── seller/                    # Seller dashboard, products, orders, withdraw
│   │   ├── shop/[username]/           # Public seller storefront
│   │   ├── signup, signin, profile/   # Auth pages
│   │   ├── wallet/                    # Buyer-side embedded Breez wallet
│   │   ├── globals.css                # Tailwind 4 @theme tokens
│   │   ├── layout.tsx
│   │   └── sw.ts                      # Serwist service worker entry
│   ├── components/                    # Shared UI (shadcn primitives + composed)
│   ├── hooks/                         # use-mobile, use-toast, …
│   ├── lib/
│   │   ├── api/                       # Typed client wrappers (auth, products, commerce, upload)
│   │   ├── auth/                      # crypto, keygen, sign, storage, use-session
│   │   ├── db.ts                      # Prisma client
│   │   ├── session.ts                 # HMAC-signed cookie sessions
│   │   ├── fetcher.ts                 # Typed fetch + SWR fetcher
│   │   ├── api-error.ts               # Shared ApiError + handler
│   │   ├── cloudinary.ts              # Signed-upload helpers
│   │   └── currency.ts                # Sats ↔ NGN, demo rate
│   ├── services/                      # Domain logic — backend only
│   │   ├── auth/                      # Slug derivation, signup, login-with-signed-challenge
│   │   ├── catalog/                   # Products, seller storefront, repository
│   │   ├── commerce/                  # Orders, ledger, pending-payments, state machine
│   │   ├── lightning/                 # Breez SDK wrapper (platform wallet)
│   │   ├── nostr/                     # publisher, signing, events, client (SimplePool)
│   │   ├── payout/                    # Bitnob sandbox client
│   │   └── pricing/                   # CoinGecko BTC/NGN rate
│   ├── store/                         # Zustand stores (session, …)
│   ├── types/                         # Shared TS types (single source of truth)
│   ├── validators/                    # Zod schemas for every API boundary
│   └── instrumentation.ts             # Next.js boot hook — register Breez event handler
├── eslint.config.mjs                  # ESLint 9 flat config
├── next.config.js                     # Conditional Serwist + Vercel modifyConfig
├── tsconfig.json
├── package.json
└── Bitscy_Integration_Flow.md         # Authoritative architecture spec
```

---

## Configuration

### Environment variables

| Variable | Description | Required |
|---|---|---|
| `DATABASE_URL` | Pooled Postgres URL (Supabase / Neon) | ✅ |
| `DIRECT_URL` | Direct Postgres URL (for migrations) | ✅ |
| `SESSION_SECRET` | HMAC secret for signed session cookies | ✅ |
| `SYSTEM_NSEC` | Server-side Nostr key for system-signed events (anonymous buyers, etc.) | ✅ |
| `NEXT_PUBLIC_NOSTR_RELAYS` | Comma-separated relay URLs | ✅ |
| `NEXT_PUBLIC_APP_URL` | Public app URL | ✅ |
| `CLOUDINARY_CLOUD_NAME` | Cloudinary cloud name | ✅ |
| `CLOUDINARY_API_KEY` | Cloudinary API key | ✅ |
| `CLOUDINARY_API_SECRET` | Cloudinary API secret | ✅ |
| `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME` | Cloudinary cloud name (client) | ✅ |
| `BREEZ_API_KEY` | Breez SDK Liquid API key | ✅ |
| `BREEZ_NETWORK` | `signet` or `mainnet` | ✅ |
| `PLATFORM_BREEZ_MNEMONIC` | 12-word mnemonic for the platform wallet | ✅ |
| `BITNOB_CLIENT_ID`, `BITNOB_CLIENT_SECRET` | Bitnob sandbox credentials | ✅ |
| `BITNOB_WEBHOOK_SECRET` | Verifies inbound Bitnob webhooks | ✅ |
| `BITNOB_API_BASE` | Defaults to `https://sandboxapi.bitnob.co/api/v1` | ✅ |
| `DEMO_BTC_NGN_RATE` | Fixed sats↔NGN rate for the demo (default `145_000_000` per BTC) | ✅ |
| `WEB_PUSH_VAPID_PUBLIC`, `WEB_PUSH_VAPID_PRIVATE`, `WEB_PUSH_VAPID_SUBJECT`, `NEXT_PUBLIC_WEB_PUSH_VAPID_PUBLIC` | Web Push notification keys | ✅ |
| `PLATFORM_FEE_PERCENT` | Bitscy fee on each sale (default `2`) | ✅ |
| `LNBITS_URL`, `LNBITS_ADMIN_KEY` | LNBits backend (alternative Lightning provider) | optional |
| `USE_MOCK_LIGHTNING`, `USE_REAL_BITNOB` | Toggles for dev | optional |
| `CRON_SECRET` | Shared secret for `/api/cron/*` routes | optional |

### API routes

| Path | Method | Owner | Purpose |
|---|---|---|---|
| `/api/auth/signup` | POST | Catalog | Create account (client-side-crypto blob) |
| `/api/auth/challenge` | POST | Catalog | Issue login challenge (anti-enumeration) |
| `/api/auth/login` | POST | Catalog | Verify signed challenge → session cookie |
| `/api/auth/me` | GET / PATCH | Catalog | Read or update authenticated user |
| `/api/auth/logout` | POST | Catalog | Clear session |
| `/api/products` | GET / POST | Catalog | List + create products |
| `/api/products/[id]` | GET / PATCH / DELETE | Catalog | Read, update, soft-delete |
| `/api/shop/[username]` | GET | Catalog | Public seller storefront |
| `/api/upload` | POST | Catalog | Cloudinary signed-upload params |
| `/api/nostr/publish` | POST | Catalog | Publish a pre-signed event to relays |
| `/api/nostr/profile/[npub]` | GET | Catalog | Fetch kind-0 profile (cached) |
| `/api/orders` | GET / POST | Commerce | List + create orders (returns BOLT-11) |
| `/api/orders/[id]` | GET | Commerce | Role-differentiated order detail |
| `/api/orders/[id]/ship` | PATCH | Commerce | Seller marks order shipped |
| `/api/orders/[id]/retry` | POST | Commerce | New invoice for an expired order |
| `/api/lightning/verify/[paymentHash]` | GET | Commerce | Frontend polling endpoint |
| `/api/wallet/balance` | GET | Commerce | Seller's current sats balance |
| `/api/wallet/activity` | GET | Commerce | Seller ledger feed |
| `/api/payout` | POST | Commerce | Initiate NGN payout via Bitnob |
| `/api/payout/[id]` | GET | Commerce | Payout status |
| `/api/payout/history` | GET | Commerce | Seller payout log |
| `/api/seller/bank-accounts` | GET / POST | Commerce | List + add saved bank accounts |
| `/api/seller/bank-accounts/[id]` | DELETE | Commerce | Remove saved bank account |
| `/api/webhooks/bitnob` | POST | Commerce | Bitnob payout status webhook |
| `/api/notifications/subscribe` | POST | Commerce | Register Web Push subscription |
| `/.well-known/lnurlp/[username]` | GET | Commerce | LNURL-pay Lightning Address endpoint |
| `/api/admin/reconcile` | GET | Commerce | Ledger ↔ Breez wallet diff (pre-demo check) |
| `/api/cron/expire-pending` | POST | Commerce | Cron sweep of expired invoices |

---

## Usage

### For sellers

1. Open `/sell` or `/signup` and create your shop with a name + password. Your Nostr identity is generated locally.
2. Save your password — Bitscy cannot reset it.
3. List a product: title, description, up to 5 photos, naira price, category. Photos upload directly to Cloudinary; the listing publishes to Nostr in the same request.
4. Share `bitscy.com/shop/<your-slug>` with buyers.
5. When a sale lands, the dashboard updates instantly: balance, total earned, recent orders.
6. Decrypt the buyer's shipping address (only you can — NIP-04), ship the piece, mark as shipped.
7. Withdraw to your Nigerian bank account: pick a saved bank, enter an amount, confirm. Bitscy routes the sats through Bitnob's sandbox; NGN reaches the bank in production.

### For buyers

1. Browse `/marketplace`. Filter by category. No account needed to look.
2. Tap a piece. Read the description. Tap **Buy**.
3. Sign up (if you don't already have an account — it's the same client-side-crypto flow, just with `role: 'BUYER'`).
4. Scan the QR with any Lightning wallet, or tap **Open in wallet** for a lightning: deep-link. Pay.
5. The page detects settlement within seconds and routes you to a success screen.
6. Track the order under `/buyer/orders`.

---

## Development

### Scripts

```bash
pnpm dev              # Start dev server (Turbopack)
pnpm build            # Production build (webpack — required for Serwist)
pnpm start            # Start the production build
pnpm lint             # ESLint 9 flat config
pnpm typecheck        # tsc --noEmit
pnpm format           # Prettier
pnpm test             # Vitest unit + integration suites
pnpm test:watch
pnpm test:coverage
pnpm db:push          # Push schema (no migration file)
pnpm db:migrate       # Create + apply a tracked migration
pnpm db:studio        # Prisma Studio UI
pnpm db:seed          # Seed demo data
```

### Local Lightning testing

The default dev experience uses LNBits (a Lightning backend that supports both real testnet and mock modes). If you don't want to run Lightning at all:

- `USE_MOCK_LIGHTNING=1` skips Breez init entirely; invoices come from a stub
- `LNBITS_ADMIN_KEY` left unset → `src/instrumentation.ts` logs a warning and proceeds without registering the event listener (auth, products, profile still work fine)

For a real settlement test, point Breez at signet or mainnet (start small — `DEMO_BTC_NGN_RATE` keeps the on-screen values matched).

### Database migrations

Two paths:

- **`pnpm db:push`** for the hackathon-velocity flow. Schema changes apply without producing migration files. Fast, but no history.
- **`pnpm db:migrate`** for tracked migrations once the schema stabilizes.

### Tests

```bash
pnpm test           # vitest run
pnpm test:coverage  # with v8 coverage
```

Integration tests under `src/__tests__/commerce/` and `src/__tests__/api/` hit a real Postgres test database (see `src/__tests__/integration-setup.ts`).

---

## Deployment

### Vercel (recommended)

1. Connect this repo to a Vercel project.
2. Set all environment variables from the [Configuration](#configuration) table.
3. Push to `main` — Vercel runs `prisma generate && next build --webpack` automatically.

The build script intentionally passes `--webpack` because Serwist's default mode is webpack-only ([serwist/serwist#54](https://github.com/serwist/serwist/issues/54)). Don't remove the flag.

### Other platforms

Any Node 22+ host with a Postgres connection works. The app is a stateless Next.js server with a few in-memory caches (CoinGecko rate, Breez SDK instance). Cron jobs (`/api/cron/expire-pending`) need a scheduler — Vercel Cron, GitHub Actions, or your platform's equivalent.

---

## Contributing

We welcome contributions. The codebase is organised around three roles — each owns a vertical slice from API routes down to UI:

| Role | Owns | Code lives in |
|---|---|---|
| **Catalog** | Products, seller profiles, auth, Nostr publishing, Cloudinary uploads | [`src/services/catalog/`](src/services/catalog/), [`src/services/auth/`](src/services/auth/), [`src/services/nostr/`](src/services/nostr/), `src/app/api/{products,shop,upload,nostr,auth}/` |
| **Commerce** | Orders, Lightning, ledger, payouts, Bitnob, CoinGecko, push | [`src/services/commerce/`](src/services/commerce/), [`src/services/lightning/`](src/services/lightning/), [`src/services/payout/`](src/services/payout/), [`src/services/pricing/`](src/services/pricing/), `src/app/api/{orders,lightning,payout,wallet,seller,webhooks,notifications}/` |
| **Experience** | Pages, components, PWA, design system, client-side crypto, deployment | [`src/app/`](src/app/) (except `/api/`), [`src/components/`](src/components/), [`src/lib/`](src/lib/), [`src/store/`](src/store/) |

The authoritative architecture spec is [`Bitscy_Integration_Flow.md`](Bitscy_Integration_Flow.md).

Conventions:

- Branch naming: `feat/<short-description>` for features, `chore/<…>`, `fix/<…>`.
- Commit messages: imperative, lowercase first word. PRs are squash-merged.
- Every PR must pass `pnpm typecheck` and `pnpm lint`.

---

## License

MIT — see [`LICENSE`](LICENSE).

---

## Acknowledgments

- [Breez](https://breez.technology) — Lightning SDK that makes self-custody approachable
- [Bitnob](https://bitnob.com) — Lightning-to-naira off-ramp for Nigerian users
- [Nostr](https://nostr.com) — decentralized identity and event protocol
- [Cloudinary](https://cloudinary.com) — image hosting + signed direct uploads
- [Supabase](https://supabase.com) — Postgres hosting
- [Next.js](https://nextjs.org), [Tailwind CSS](https://tailwindcss.com), [shadcn/ui](https://ui.shadcn.com), [Radix UI](https://www.radix-ui.com) — the foundation
